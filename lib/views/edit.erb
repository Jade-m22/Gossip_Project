<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Édition du potin</title>
  </head>
  <body>
    <div class="container mt-5">
      <div class="border p-4 rounded shadow-sm">
        <h1 class="fw-bold text-primary">Édition du potin</h1>
        
        <form action="/gossips/<%= params['id'] %>/edit" method="post">
          <div class="mb-3">
            <label for="gossip_author" class="form-label">Auteur :</label>
            <input type="text" class="form-control" id="gossip_author" name="gossip_author" value="<%= gossip.author %>">
          </div>

          <div class="mb-3">
            <label for="gossip_content" class="form-label">Contenu :</label>
            <textarea id="gossip_content" name="gossip_content" class="form-control" rows="5"><%= gossip.content %></textarea>
          </div>

          <button type="submit" class="btn btn-primary w-100">Mettre à jour</button>
        </form>

        <a href="/" class="btn btn-secondary mt-3 w-100">Retour</a>
      </div>
    </div>
  </body>
</html>
