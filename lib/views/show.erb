<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Potin - <%= gossip.author %></title>
  </head>
  <body>
    <div class="container mt-5">
      <div class="border p-4 mb-4 rounded shadow-sm">
        <h1 class="fw-bold text-primary">Voici le potin de <%= gossip.author %></h1>
        <p class="lead">Le contenu du potin : <%= gossip.content %></p>
        <a href="/gossips/<%= params['id'] %>/edit/" class="btn btn-warning">Éditer ce potin</a>
        <br/>
        <a href="/" class="btn btn-secondary mt-2">Retour à la liste des potins</a>
      </div>

      <div class="border p-4 mb-4 rounded shadow-sm">
        <h2 class="fw-bold text-success">Commentaires :</h2>
        <% if comments.empty? %>
          <p>Aucun commentaire pour ce potin.</p>
        <% else %>
          <ul class="list-unstyled">
            <% comments.each do |comment| %>
              <li class="border-bottom pb-2 mb-2"><i>"<%= comment %>"</i></li>
            <% end %>
          </ul>
        <% end %>
      </div>

      <div class="border p-4 rounded shadow-sm">
        <h3 class="fw-bold text-info">Ajouter un commentaire :</h3>
        <form action="/gossips/<%= params['id'] %>/comments" method="post">
          <textarea name="comment_content" class="form-control mb-3" rows="4" placeholder="Ton commentaire ici"></textarea>
          <input type="submit" value="Envoyer" class="btn btn-primary w-100">
        </form>
      </div>
    </div>
  </body>
</html>